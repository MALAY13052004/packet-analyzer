services:
  - type: web
    name: packet-analyzer
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PACKETLAB_CAPTURE_ENABLED
        value: "true"
      - key: PACKETLAB_CAPTURE_ENVIRONMENT
        value: "cloud"
